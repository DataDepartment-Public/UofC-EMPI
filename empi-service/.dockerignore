# Runtime state — never baked into the image, mounted as volumes instead.
data/
models/
logs/

.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

.ipynb_checkpoints/
notebooks/

.pytest_cache/
.coverage
htmlcov/
tests/

.ruff_cache/
.mypy_cache/

# scripts/ IS needed in the image -- docker-compose.yml's own comment tells
# operators to run `docker compose exec empi-service python scripts/init_db.py`
# for schema setup, and the Dockerfile COPYs it in (see below). research/ is
# ad-hoc analysis, not part of that ops surface -- excluded on its own.
scripts/research/
docs/

.env
.env.example
.git/
.gitignore
.DS_Store

Dockerfile
.dockerignore
